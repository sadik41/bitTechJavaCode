package com.bit.common;

public class OverridingConceot  {

	// when we do method overriding only thing we can change is the body
	// we must create is a relation/inherit to do method overriding
	// method name, access modifier and parameter must be the same
}
